# Docker Compose Environment Variables
NODE_ENV=development

# Docker build target (production or development)
DOCKER_TARGET=development

# Frontend port (default: 4173 for production, 5173 for development)
FRONTEND_PORT=4173

# Build target (production or development) - DEPRECATED, use DOCKER_TARGET instead
BUILD_TARGET=production
